import { QuestionAnswerData } from './question-answer-data';

describe('QuestionAnswerData', () => {
  it('should create an instance', () => {
    expect(new QuestionAnswerData()).toBeTruthy();
  });
});
